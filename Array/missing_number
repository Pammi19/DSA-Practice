class Solution {
  public:
    int missingNum(vector<int>& arr) {
        
        long sum=0;
        long n=arr.size(); 
        long total=(n+1)*(n+2)/2; 
        for(long i : arr)
         sum+=i;
       
        return total-sum;   
    }
};
